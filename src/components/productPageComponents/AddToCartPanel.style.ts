import styled from "styled-components";
import Col from "../baseComponents/Col.style";

const StyledAddToCartPanel = styled(Col)`
  margin: 2rem 0 0 2rem;
  .product__type {
    margin-bottom: 0;
    font-size: 0.8rem;
  }
`;

export default StyledAddToCartPanel;
