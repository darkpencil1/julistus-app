import styled from "styled-components";

const StyledHeartMask = styled.svg`
  border: 3px solid red;
  position: absolute;
  margin: auto;
  top: 50%;
  fill: #555555;
`;

export default StyledHeartMask;
