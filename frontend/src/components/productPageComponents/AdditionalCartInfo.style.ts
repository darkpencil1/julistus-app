import styled from "styled-components";

const StyledAdditionalCartInfo = styled.div`
  margin: 2rem 0;
  .cartInfo__thumbnail {
    width: 2rem;
    height: auto;
  }
`;

export default StyledAdditionalCartInfo;
